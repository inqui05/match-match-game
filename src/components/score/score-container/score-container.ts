import './score-container.sass';
import { BaseComponent } from '../../base-component';

export class ScoreContainer extends BaseComponent {
  constructor() {
    super('div', ['score-container']);
  }
}
