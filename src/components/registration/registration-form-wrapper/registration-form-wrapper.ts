import './registration-form-wrapper.sass';
import { BaseComponent } from '../../base-component';

export class RegistrationFormWrapper extends BaseComponent {
  constructor() {
    super('div', ['registration__form-wrapper']);
  }
}
