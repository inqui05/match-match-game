import './registration-field-wrapper.sass';
import { BaseComponent } from '../../base-component';

export class RegistrationFieldWrapper extends BaseComponent {
  constructor() {
    super('div', ['registration__field-wrapper']);
  }
}
