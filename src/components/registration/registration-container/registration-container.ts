import './registration-container.sass';
import { BaseComponent } from '../../base-component';

export class RegistrationContainer extends BaseComponent {
  constructor() {
    super('div', ['registration__container']);
  }
}
