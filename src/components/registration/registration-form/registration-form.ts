import './registration-form.sass';
import { BaseComponent } from '../../base-component';

export class RegistrationForm extends BaseComponent {
  constructor() {
    super('form', ['registration__form']);
  }
}
