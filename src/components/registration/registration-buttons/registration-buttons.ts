import './registration-buttons.sass';
import { BaseComponent } from '../../base-component';

export class RegistrationButtons extends BaseComponent {
  constructor() {
    super('div', ['registration__buttons']);
  }
}
