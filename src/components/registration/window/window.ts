import './window.sass';
import { BaseComponent } from '../../base-component';

export class RegistrationWindow extends BaseComponent {
  constructor() {
    super('div', ['registration__window']);
  }
}
