import './playing-container.sass';
import { BaseComponent } from '../../base-component';

export class PlayingContainer extends BaseComponent {
  constructor() {
    super('div', ['playing-container']);
  }
}
